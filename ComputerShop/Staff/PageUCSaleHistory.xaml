<UserControl x:Class="Dashboard.Staff.PageUCSaleHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dashboard.Staff"
             mc:Ignorable="d"  >
    <Grid x:Name="GridShowProduct">
        <ScrollViewer x:Name="MyViewer"
                      Margin="0 10 0 0"
                      CanContentScroll="True"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Height="{Binding ElementName=GridShowProduct, Path=Height}">
            <StackPanel x:Name="RenderPages" Height="{Binding ElementName=MyViewer, Path=ViewportHeight}" >

            </StackPanel>
        </ScrollViewer>

    </Grid>

</UserControl>
